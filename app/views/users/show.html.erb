
<style>
/* Basic styling for the modal */
.modal {
    display: none; /* Hidden by default */
    position: fixed; /* Stay in place */
    z-index: 1; /* Sit on top */
    left: 0;
    top: 0;
    width: 100%; /* Full width */
    height: 100%; /* Full height */
    overflow: auto; /* Enable scroll if needed */
    background-color: rgb(0,0,0); /* Fallback color */
    background-color: rgba(0,0,0,0.4); /* Black w/ opacity */
}
.modal-content {
    background-color: #fefefe;
    margin: 15% auto; /* 15% from the top and centered */
    padding: 20px;
    border: 1px solid #888;
    width: 80%; /* Could be more or less, depending on screen size */
}
.close {
    color: #aaa;
    float: right;
    font-size: 28px;
    font-weight: bold;
}
.close:hover,
.close:focus {
    color: black;
    text-decoration: none;
    cursor: pointer;
}

/* Button styles */
.header {
    display: flex;
    justify-content: flex-end;
    gap: 10px; /* Space between buttons */
    margin: 10px;
}
.btn {
    padding: 10px 15px; /* Add some padding */
    border: none;
    border-radius: 5px; /* Rounded corners */
    color: white;
    cursor: pointer;
    font-size: 16px; /* Larger font size */
}
.btn-profile {
    background-color: #4CAF50; /* Green background */
}
.btn-logout {
    background-color: #f44336; /* Red background */
}
.btn-create {
    background-color: #008CBA; /* Blue background (for the Create Image button) */
}
</style>

<body>
  <div class="top-left-image">
    <%= image_tag "ussf_logo.png", alt: "Logo", class: "ussf-logo"%>
  </div>
  <div class="header">
    <button id="profileBtn" class="btn btn-profile">Profile</button>
    <%= link_to "Create Image", new_tag_path, class: "btn btn-create" %>
    <%= link_to "Logout", logout_path, class: "btn btn-logout" %>
  </div>

  <!-- The Modal -->
  <div id="myModal" class="modal">
    <div class="modal-content">
      <span class="close" id="closeModal">&times;</span>
      <h2>User Details</h2>
      <p>Name: <%= @current_user.first_name %> <%= @current_user.last_name %></p>
      <p>Email: <%= @current_user.email %></p>
      <!-- Add other user details as needed -->
    </div>
  </div>

  <div>
  <h2>Images for Run Time Objects</h2>
  <% if @image_list.present? %>
    <% @image_list.each do |image| %>
      <div class="image-item">
        <p><strong>Tag:</strong> <%= link_to image.tag, tag_path(image.tag) %></p>
        <hr>
      </div>
    <% end %>
  <% else %>
    <p>No images found for this user's Run Time Objects.</p>
  <% end %>
</div>

  <script>
    // Get the modal
    var modal = document.getElementById("myModal");

    // Get the button that opens the modal
    var btn = document.getElementById("profileBtn");

    // Get the <span> element that closes the modal
    var span = document.getElementById("closeModal");

    // When the user clicks the button, open the modal 
    btn.onclick = function() {
      modal.style.display = "block";
    }

    // When the user clicks on <span> (x), close the modal
    span.onclick = function() {
      modal.style.display = "none";
    }

    // When the user clicks anywhere outside of the modal, close it
    window.onclick = function(event) {
      if (event.target == modal) {
        modal.style.display = "none";
      }
    }
  </script>
</body>

