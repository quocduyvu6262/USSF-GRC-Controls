<h1>Edit Runtime Object</h1>

<%= form_with(model: @run_time_object, local: true) do |form| %>
  <% if @run_time_object.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@run_time_object.errors.count, "error") %> prohibited this runtime object from being saved:</h2>

      <ul>
        <% @run_time_object.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>

<%= link_to 'Back', run_time_object_path(@run_time_object) %>